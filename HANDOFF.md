# Handoff: поддержка восьмиканальной платы ESP-07S

Обновлено: 2026-08-27

## Цель

Добавить в этот репозиторий стабильную прошивку для соседней восьмиканальной
релейной платы на ESP8266 ESP-07S, сохранив существующие образы ESP-01, D1 mini
и DS18B20 совместимыми по поведению и MQTT-топикам.

Рабочая ветка: `feature/esp07s-relay8`.

## Область работы

- новый профиль платы `esp07s` с корректной разметкой 1 МБ и контролем OTA;
- новое окружение `esp07s_relay8` без адресов и секретов в репозитории;
- отдельная реализация устройства `device_relay_bank` для восьми каналов;
- несколько независимых сущностей Home Assistant на одном физическом устройстве;
- портал, подписи каналов, восстановление состояний и таймеры с выбором канала;
- native-тесты, сборки всех конечных образов, CI, release и runbook.

Вне области без отдельного разрешения: `Gate`, прошивка DS18B20 и любые
загрузки на физические устройства. Проверка на железе, когда до неё дойдём,
начинается с релейной платы.

## План и состояние

- [x] Осмотрена архитектура текущего репозитория и соседнего проекта.
- [x] Создана ветка `feature/esp07s-relay8`.
- [x] Создан этот журнал работы.
- [x] Добавить профиль `esp07s` и окружение `esp07s_relay8`.
- [x] Обобщить контракт устройства и MQTT-транспорт на несколько сущностей.
- [x] Сохранить прежнюю ветку построения MQTT-топиков одноканального реле и датчика.
- [x] Реализовать восьмиканальное устройство и его настройки.
- [x] Добавить целевой канал в суточные таймеры банка реле.
- [x] Добавить тесты и обновить архитектурную документацию, CI и release.
- [x] Выполнить `pio test -e native`.
- [x] Собрать все конечные образы и проверить каждую строку `OTA headroom`.
- [x] Подготовить чек-лист проверки релейной платы без фактической прошивки.
- [x] Исправить обнаруженное на OTA несоответствие Flash 4 МБ/1 МБ.
- [x] Пересобрать исправленную версию 4.0.1 и проверить заголовок BIN.

## Принятые решения

1. Соседний каталог называется `ESP32-Relay-Portal`, но текущая плата и код
   используют ESP8266 ESP-07S. Поддержка настоящего ESP32 в эту ветку не входит.
2. Восьмиканальный банк будет отдельным типом устройства, а не набором
   условных веток внутри стабильного одноканального `device_relay`.
3. MQTT должен представлять восемь стабильных сущностей с идентификаторами
   `relay_1` ... `relay_8`. Пользовательские подписи не должны участвовать в
   топиках или `unique_id`.
4. Состояние восьми каналов публикуется одним JSON в общем state topic;
   discovery и command topic у каждого канала свои, availability общий.
5. Для одноканальных устройств пути топиков, `unique_id` и `object_id` должны
   остаться прежними.
6. Физическая релейная плата сообщает реальный объём Flash 1 МБ. Профиль
   ESP-07S поэтому принудительно использует `eagle.flash.1m64.ld`: в отличие от
   старой разметки `1m` она сохраняет 64 КБ под используемый прошивкой LittleFS.

## Найденные трудности и риски

- Восьмиканальные изменения соседнего проекта находятся в незакоммиченной
  рабочей копии. Она используется только как источник распиновки и требований.
- Текущий `CoreMqttClient` хранит ровно одну подписку и один callback. Для
  восьми командных топиков потребуется ограниченный таблицей набор подписок.
- Текущий статический контракт описывает одну Home Assistant сущность. Его
  расширение затрагивает общее ядро, поэтому обязательны сборки всех образов.
- GPIO0 и GPIO15 являются загрузочными GPIO ESP8266. Корректность их внешних
  подтяжек определяется схемой платы и не может быть исправлена прошивкой.
- Старый восьмиканальный код создаёт восемь объектов с GPIO0 до `setup()` и
  таймерами управляет только первым реле. Эту реализацию переносить нельзя.
- Текущий образ реле занимает около 472 КБ. Для ESP-01 и ESP-07S с 1 МБ запас
  мал, поэтому даже небольшие изменения должны контролироваться обеими
  сборками.
- Пример адреса брокера заменён с адреса локальной сети на нейтральный
  `192.168.1.10`, чтобы шаблон не подсказывал конкретную инфраструктуру.
- `pio project config` разобрал новое окружение успешно, но завершение команды
  попыталось записать телеметрию в `~/.platformio/.cache`, что запрещено
  песочницей. Для итоговых запусков PlatformIO может потребоваться разрешение
  на его штатный домашний кеш; это не ошибка конфигурации проекта.

## Промежуточные проверки

- После расширения MQTT-контракта: `pio test -e native` -- 153/153 успешно.
- `esp01_relay` после расширения ядра: сборка успешна, RAM 34 616 байт,
  OTA 473 808 из 481 280 байт, осталось 7 472 байта.
- `esp07s_relay8`: первая сборка успешна, RAM 35 144 байта, OTA 475 296 из
  1 044 464 байт, осталось 569 168 байт. Эта проверка обнаружена как ошибочная:
  профиль платы описывал 4 МБ, а физический модуль имеет 1 МБ.

## Итоговые проверки версии 4.0.0 (заменена исправлением 4.0.1)

- `git diff --check` -- успешно.
- `pio test -e native` -- 157/157 успешно.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 456 / 1 044 464, осталось 569 008 байт.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.

## Исправление версии 4.0.1

- OTA-загрузка образа 4.0.0 была отклонена устройством: `420ERROR[9]: new Flash
  config wrong real: 1048576`.
- Причина: манифест PlatformIO для `esp07s` по умолчанию кодировал в заголовке
  образа 4 МБ, тогда как устройство обнаружило 1 МБ (1 048 576 байт).
- Профиль переведён на `eagle.flash.1m64.ld`, OTA-предел снижен до 481 280 байт.
- Версия поднята до 4.0.1, чтобы исправленный BIN не путался с образом 4.0.0.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 456 / 481 280, осталось 5 824 байта.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.
- Заголовок `ESP_Relay_ESP07S_8ch_v4.0.1.bin` начинается с `e9 02 00 20`;
  код размера Flash `2` соответствует 1 МБ в ESP8266 core.
- PlatformIO продолжает показывать `HARDWARE: ... 4MB Flash` из манифеста
  `esp07s`, но linker script, доступный размер 958 448 байт и заголовок BIN
  подтверждают фактическую конфигурацию 1 МБ.
- `pio test -e native` -- 157/157 успешно; `git diff --check` -- успешно.

На момент проверки 4.0.1 на физические устройства ничего не загружалось.

## Расширение таймеров версии 4.1.0

- В список целей таймера после реле 1–8 добавлен пункт `All relays` со
  значением 8. Существующие сохранённые значения 0–7 не меняют смысл.
- Групповая цель выполняет действие для всех восьми каналов за один проход
  расписания и одну последующую синхронизацию состояния/MQTT.
- Каналы в `Button mode` при групповой цели сохраняют прежнюю семантику и
  всегда получают импульс `On`; обычные каналы выполняют выбранные
  `On`, `Off` или `Toggle`.
- `pio test -e native` -- 160/160 успешно.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 728 / 481 280, осталось 5 552 байта.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.
- Заголовок ESP-07S остаётся `e9 02 00 20` (Flash 1 МБ).
- Аппаратная проверка группового таймера ещё не выполнялась; сценарий добавлен
  в `docs/runbooks/verification.md`.

## Загрузка на ESP-07S

- 2026-08-06 образ `ESP_Relay_ESP07S_8ch_v4.1.0.bin` успешно загружен по OTA
  с локальными параметрами из `platformio_override.ini`.
- OTA-загрузчик принял все 475 728 байт и вернул `Result: OK`.
- После перезагрузки портал отвечает `HTTP 200` и показывает версию `4.1.0`.
- Каналы реле после загрузки программно не переключались, чтобы не воздействовать
  на возможную подключённую нагрузку.
- `Gate` и DS18B20 не затрагивались.

## Следующий шаг

Выполнить аппаратный smoke-тест ESP-07S при безопасной нагрузке: проверить
каналы 1–8, новую цель `All relays` и холодный старт GPIO0/GPIO15. `Gate` и
DS18B20 в эту проверку не входят.

## Локальная массовая OTA-загрузка

- Создан локальный `tools/fleet_upload.py`; по решению владельца утилита
  остаётся целиком в `.gitignore`.
- Инвентарь релейных плат и датчиков вынесен из кода в локальный
  `tools/fleet.ini`. Файл с адресами игнорируется Git, а безопасный формат
  задокументирован в отслеживаемом `tools/fleet.ini.example`.
- Без `--execute` утилита только показывает план. Перед OTA она сначала
  собирает все нужные окружения, затем проверяет доступность и обновляет
  устройства последовательно, после каждого ожидая портал с новой версией.
- При смешанном выборе утилита сначала обрабатывает все реле, затем датчики;
  внутри каждой группы сохраняется порядок секций `fleet.ini`.
- Проверены `--list`, dry-run всех шести устройств и выбор одного датчика:
  инвентарь читается из INI, смешанный план содержит сначала четыре реле, затем
  два датчика; сборка и OTA во время проверки не запускались.
- По умолчанию выбираются только реле. Для DS18B20 обязательно явно передать
  `--allow-sensors`; дополнительно проверяется отсутствие незакоммиченных
  изменений в коде прошивки.
- До версии 4.2.0 D1 mini с DS18B20 была внесена в локальный инвентарь как
  заблокированная цель: утилита не позволяла отправить на неё образ ESP-01.
- Dry-run группы реле, вывод инвентаря и защитные отказы для датчиков проверены;
  массовая загрузка утилитой не запускалась.

## Окружение D1 mini с DS18B20, версия 4.2.0

- Добавлено окружение `d1_mini_ds18b20` на профиле `[d1_mini]` с OneWire на
  D4/GPIO2 и отдельным образом `ESP_DS18B20_D1_mini_v4.2.0.bin`.
- `device_ds18b20` теперь требует явный `ONE_WIRE_PIN`: ESP-01 передаёт GPIO2,
  D1 mini — D4. Скрытого пина по умолчанию больше нет.
- Новый образ добавлен в CI, release workflow, архитектуру и runbook.
- Локальная fleet-утилита разблокировала D1 mini DS18B20, но массовая загрузка
  и загрузка на датчики не выполнялись.
- `pio test -e native` -- 160/160 успешно; `git diff --check` -- успешно.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 728 / 481 280, осталось 5 552 байта.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `d1_mini_ds18b20` -- 471 728 / 1 044 464, осталось 572 736 байт.
- Заголовок D1 mini DS18B20 начинается с `e9 02 02 40` и кодирует штатные
  4 МБ Flash; ESP-01 DS18B20 остаётся `e9 02 00 20` с 1 МБ.
- Распиновка `d1_mini_relay` изменена с D2/GPIO4 на D1/GPIO5 по решению
  владельца; это не затрагивает отдельное окружение `d1_mini_ds18b20` на D4.

## Уточнение конфигурации D1 mini и примера брокера

- `d1_mini_relay` использует D1/GPIO5 вместо D2/GPIO4; поясняющий комментарий
  в `platformio.ini` приведён в соответствие с флагом сборки.
- `tools/broker.ini.example` содержит нейтральный пример `192.168.1.10`.
- Локальные `platformio_override.ini`, `tools/broker.ini` и
  `tools/fleet_upload.py` остаются под `.gitignore` и в коммит не входят.
- `pio run -e d1_mini_relay` -- успешно; OTA-образ 473 824 из 1 044 464 байт,
  осталось 570 640 байт. `git diff --check` -- успешно.

## Релизная проверка 4.2.0

- Документация сверена с `platformio.ini`: README, архитектура, ADR и runbook
  теперь одинаково указывают D1/GPIO5 для реле D1 mini и D4/GPIO2 для его
  DS18B20. В описании 4.2.0 отмечена несовместимая смена провода D2 → D1.
- Проверены все локальные Markdown-ссылки — отсутствующих целей нет.
- Чистый прогон `pio test -e native` — 160/160 успешно.
- `esp01_relay` — 473 808 / 481 280, осталось 7 472 байта.
- `esp01_relay_metrics` — 474 448 / 481 280, осталось 6 832 байта.
- `d1_mini_relay` — 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` — 475 728 / 481 280, осталось 5 552 байта.
- `esp01_ds18b20` — 471 728 / 481 280, осталось 9 552 байта.
- `d1_mini_ds18b20` — 471 728 / 1 044 464, осталось 572 736 байт.
- Заголовки ESP-01 и ESP-07S кодируют 1 МБ (`e9 02 00 20`), заголовки D1 mini
  — 4 МБ (`e9 02 02 40`). Все шесть окружений собраны успешно.
- Перед слиянием `origin/main` совпадает с локальным `main`, тег
  `version_4.2.0` отсутствует.

## Проверка gzip-OTA на реле, 2026-08-07

Проверка выполнена при обсуждении самообновления устройства с GitHub: запаса под
OTA у однометровых плат почти нет (`esp01_relay` — 7 472 байта, `esp07s_relay8`
— 5 552), и вопрос был, можно ли поднять потолок сжатием образа. Работа
исследовательская, в репозиторий изменений не вносилось.

### Что проверено на железе

Оба теста прошли на реле ESP-01 по адресу из `platformio_override.ini`, версия
в прошивке маркировалась временным окружением, чтобы успех заливки нельзя было
спутать с отсутствием изменений. `Gate` и датчики не затрагивались.

- Тест 1, сам механизм: образ 473 808 байт, отправленный через espota как
  gzip 326 024 байта, загрузился; портал вернул версию `4.2.0gz`.
- Тест 2, потолок: образ с балластом **539 424 байта — на 58 144 больше
  нынешнего предела 481 280** — сжат до 369 650 байт (68,5 %), загрузился и
  работает. Портал отдаёт `4.2.0fat`, индикаторы Wi-Fi и MQTT зелёные,
  страница реле с `Push` и таймерами на месте.
- Возврат несжатого стока с «толстой» прошивки отклонён устройством:
  `ERROR[4]: Not Enough Space`. Сжатый сток прошёл, реле возвращено на 4.2.0.
- Сжатие образов этого проекта — 68,5–68,9 % для всех пяти окружений.

### Почему потолок сдвигается

`Update.begin(size)` размещает область стейджинга в конце области скетчей,
отступив назад на *объявленный* размер, а eboot распаковывает от нуля вверх.
Отсюда два ограничения при области 962 560 байт (`eagle.flash.1m64.ld`):

- стейджинг обязан помещаться за текущим скетчем: `gz ≤ 962 560 − текущий`;
- распаковка не должна догнать источник: `raw < 962 560 − gz`.

В тесте 2 стейджинг встал на 589 824, распакованный образ занял 0…539 424,
запас 50 400 байт. Обе границы сходятся в устойчивом потолке **около 565 КБ
вместо 481 280** — примерно плюс 85 КБ для ESP-01 и ESP-07S, у которых
разметка одинаковая.

### Ограничение, которое важнее самого выигрыша

Размер объявляет тот, кто заливает, и выигрыш достаётся **только пути
espota/ArduinoOTA**: он передаёт настоящий размер файла. GyverPortal в
`CustomOTA.h` объявляет `getFreeSketchSpace()` целиком, поэтому стейджинг
встаёт сразу за текущим скетчем и на распакованный образ остаётся прежние
~475 КБ — через страницу портала потолок не двигается.

При этом `copy_raw` в `bootloaders/eboot/eboot.c` не проверяет перекрытие:
распаковка пишет вверх, пока не кончится длина из хвоста gzip. Значит gz-образ
крупнее ~475 КБ, залитый через `/ota_update`, затрёт собственный источник на
середине — это кирпич, лечится только UART. Вывод сделан по исходникам ядра и
намеренно **не** проверялся на железе.

Отдельно: для gz-образов `Updater` не патчит байты flash mode/size в заголовке
(патч пропускается, когда первый байт равен `0x1f`). Сжатый образ обязан быть
собран под ту же плату — для наших окружений это выполняется само собой.

### Что из этого следует

- Публиковать в релизе `.bin.gz` рядом с `.bin`: это и вдвое меньше трафика,
  и единственный способ получить запас на однометровых платах.
- Прежде чем поднимать `custom_ota_max_size`, заменить обработчик загрузки
  портала своим — объявляющим настоящий размер и отказывающим образу, который
  не поместится. Иначе сборка разрешит образ, безопасно заливаемый лишь одним
  из двух путей.
- С «толстой» прошивки обновляться можно только сжатыми образами: свободное
  место под стейджинг равно `962 560 − текущий скетч`. Это надо учитывать в
  `tools/fleet_upload.py` и в runbook, когда дойдёт до дела.
- По флешу освободившиеся ~85 КБ примерно закрывают запрос BearSSL (60–70 КБ),
  то есть самообновление по HTTPS прямо с устройства перестаёт быть безнадёжным
  и для ESP-01. Остаётся вопрос RAM: handshake без MFLN требует около 25 КБ
  кучи, а свободная куча на реле не измерялась. Это следующий замер.

### Временные артефакты проверки (сняты)

Маркерные окружения `esp01_relay_gz` и `esp01_relay_fat` добавлялись в локальный
`platformio_override.ini`, балласт — во временный `src/fat_pad.cpp` (срез
настоящей прошивки, чтобы сжимался как прошивка). После проверки всё удалено,
`git status` чист, реле работает на стоковой 4.2.0.

## Обновление с GitHub из портала, ветка `feature/portal-github-update`

Реализован вариант, при котором в интернет ходит не устройство, а отданная им
страница. Причина выбора и отвергнутые пути — в
[ADR 0012](docs/adr/0012-portal-github-update.md).

### Что сделано

- `lib/core/core_update.h`: маршрут `/fw.js` со скриптом в PROGMEM (в 4.7.0
  скрипт уехал на зеркало, маршрута больше нет), факты
  устройства в страницу (версия, имя образа, свободное место), блок на странице
  обновления и строка-подсказка на главной.
- `extra_script.py` передаёт `-D IMAGE_NAME` из `custom_device_name` — по нему
  прошивка находит свой файл среди пяти в манифесте.
- `release.yml` собирает `manifest.json` (версия, файл, размер, md5 каждого
  образа) и публикует его вместе с бинарниками на GitHub Pages. Шаги стоят
  после создания релиза, чтобы невключённый Pages ломал зеркало, а не выпуск.
- README, ARCHITECTURE, ADR и runbook (раздел 3b) обновлены.

### Цена по флешу

| Образ | Было | Стало | Осталось |
|---|---|---|---|
| `esp01_relay` | 473 808 | 475 952 | 5 328 |
| `esp01_relay_metrics` | 474 448 | 476 608 | 4 672 |
| `esp07s_relay8` | 475 728 | 477 904 | **3 376** |
| `esp01_ds18b20` | 471 728 | 473 872 | 7 408 |
| `d1_mini_relay` | 473 824 | 475 984 | 568 480 |
| `d1_mini_ds18b20` | 471 728 | 473 872 | 570 592 |

Прибавка 2 144–2 176 байт, почти целиком текст скрипта. У `esp07s_relay8`
осталось 3 376 байт — самый узкий образ репозитория, следующая заметная правка
ядра упрётся именно в него.

### Проверено

- `pio test -e native` — 160/160; собраны все шесть окружений.
- Реле 10.0.1.86 прошито новой сборкой: `/fw.js` отдаётся (200,
  `application/javascript`, 1499 байт, `Cache-Control: max-age=86400`), обе
  страницы несут `FW={v:'4.2.0',i:'Relay',f:479232}`.
- Значение `f` сошлось с расчётом до байта: скетч 475 952 → 479 232 после
  округления, 962 560 − 479 232 = 483 328, минус 4096 и вниз до 4 КБ = 479 232.
- Скрипт, **отданный самим устройством**, прогнан в node с заглушками браузера:
  24 проверки — сравнение версий (включая 4.10.0 против 4.9.0), молчание
  главной при актуальной версии и недоступном GitHub, отказы «нет своего
  образа» и «не помещается» без отправки на устройство, граница размера,
  штатная установка с проверкой имени поля формы и имени файла.
- Путь установки проверен на железе тем же запросом, который делает браузер:
  `curl -F firmware=@...` на `/ota_update` — `Update Success!`, 476 КБ за
  15,3 секунды, реле вернулось с работающим MQTT.

### Выпуск 4.3.0

- `release_version` поднят до 4.3.0; README, примеры имён образов и пример тега
  приведены к новой версии, добавлено описание изменения.
- Pages в репозитории уже был включён, но в режиме `legacy` из ветки `main`:
  по адресу зеркала публиковался README. `deploy-pages` в этом режиме
  разворачивать артефакт отказывается, поэтому режим переведён на публикацию из
  Actions, а в workflow добавлен `enablement: true`, чтобы это состояние
  восстанавливалось само.
- В зеркало кладётся `index.html`: адрес, где раньше отдавался README, иначе
  стал бы отвечать 404.
- Первый прогон тега упал за секунду, не начав шагов: окружение `github-pages`
  по умолчанию разрешает деплой только с ветки по умолчанию, а релиз идёт с
  тега (`Tag "version_4.3.0" is not allowed to deploy to github-pages due to
  environment protection rules`). В Deployment branches and tags добавлено
  правило для тегов `version_*`; обе настройки репозитория описаны в README.
- Выпуск совпал с многочасовым сбоем GitHub Actions: события push в `main` и
  push тега потерялись, прогоны не создавались. Тег пришлось перевыложить
  после восстановления.
- Размеры образов от подъёма версии не изменились: строка версии той же длины.

### Что показал выпущенный 4.3.0

Зеркало работает: `manifest.json` отдаётся с `Access-Control-Allow-Origin: *`,
ключи совпадают с именами, зашитыми в образы. Реле на 4.2.0 видит новую версию.
Но установить её через портал оно не может, и вскрылись две вещи.

**1. Образы из CI на 3,3 КБ больше локальных.** Причина — неприкреплённая
версия платформы: локально `espressif8266` 3.2.0, в CI 4.2.1. Все цифры запаса
в этом файле сняты локально и потому оптимистичны. Реальные размеры релиза:

| Образ | Из CI | Осталось до предела |
|---|---|---|
| `esp01_relay` | 479 280 | 2 000 |
| `esp07s_relay8` | 481 168 | **112** |
| `esp01_ds18b20` | 477 216 | 4 064 |
| `d1_mini_relay` | 479 296 | 565 168 |
| `d1_mini_ds18b20` | 477 216 | 567 248 |

У ESP-07S до стены 112 байт: следующая правка ядра уронит релизную сборку.
Версии библиотек в этом репозитории прикреплены осознанно (см. комментарий у
пары MQTT/TCP), а платформа — нет, и релиз зависит от того, что CI разрешит в
день сборки. Прикрепить `platform = espressif8266@4.2.1` и пересчитать запасы.

**2. Установка через портал упирается в лишние 4 КБ.** Обработчик GyverPortal
объявляет `Update.begin()` не настоящий размер файла, а
`(getFreeSketchSpace() − 0x1000)`. У реле со сборкой 4.2.0 это 479 232 байта,
а релизный образ — 479 280. Не хватает 48 байт. Проверено скриптом, который
отдаёт само устройство, на настоящем манифесте и настоящих фактах реле:

```
страница показывает: "New version 4.3.0 available"
кнопка Install: показана
после нажатия: "Image needs 479280 B, only 479232 B free"
что-то отправлено на устройство: нет
```

Защита сработала как задумано — отказ пришёл до скачивания, на устройство не
ушло ничего. Но обновиться через портал на однометровых платах сейчас нельзя.

Лечится тем самым своим обработчиком загрузки, который уже записан в план после
проверки gzip-OTA: с настоящим размером стейджинг встаёт по адресу
962 560 − 483 328 = 479 232, ровно на границе текущего скетча, и `Update.begin`
это пропускает. Те самые 4 КБ и возвращаются. Для espota ограничение не
действует — там размер объявляется настоящий, и релизный образ ставится.

## Исправление 4.3.1

Сделано обе вещи из плана выше.

**Версия платформы прикреплена** (`espressif8266@4.2.1`). После этого локальные
образы совпали с CI байт в байт — сверено по размерам всех пяти файлов.

**Приём образа свой** — `lib/core/core_ota.h`, `portal.enableOTA()` больше не
зовётся. Размер и md5 приходят параметрами `?size=` и `?md5=` из манифеста;
параметры берутся из строки запроса, потому что аргументы формы к моменту
UPLOAD_FILE_START ещё не собраны (Parsing-impl.h:180 против :192). Ручная
заливка файлом их передать не может и работает по прежнему правилу. Решение
и отвергнутые варианты — в
[ADR 0013](docs/adr/0013-own-firmware-upload-handler.md).

Отказ от `CustomOTA` окупил свой код с лишком:

| Образ | 4.3.0 | 4.3.1 | Осталось |
|---|---|---|---|
| `esp01_relay` | 479 280 | 477 216 | 4 064 |
| `esp01_relay_metrics` | 479 920 | 477 872 | 3 408 |
| `esp07s_relay8` | 481 168 | 479 104 | **2 176** (было 112) |
| `esp01_ds18b20` | 477 216 | 475 104 | 6 176 |
| `d1_mini_relay` | 479 296 | 477 232 | 567 232 |
| `d1_mini_ds18b20` | 477 216 | 475 104 | 569 360 |

### Найденная своя ошибка

Первая версия обработчика отвечала «успех» и перезагружалась на POST **без
файла**: обработчик загрузки в этом случае не зовут ни разу, и отсутствие
ошибки выглядело как удачное обновление. Добавлен отдельный признак «образ
принят»; сценарий внесён в runbook как обязательный.

### Проверено на железе

Матрица на реле 10.0.1.86, каждая неудачная строка обязана оставить прежнюю
прошивку и ответить сразу:

| Сценарий | Ответ |
|---|---|
| страница `/ota_update` | блок GitHub и форма заливки на месте |
| ручная заливка без параметров | `Update success`, 15,3 с |
| путь скрипта с `?size=` и `?md5=` | `Update success` |
| неверный md5 | `MD5 verification failed`, без перезагрузки |
| объявлен размер меньше настоящего | `Not Enough Space`, без перезагрузки |
| POST без файла | `no firmware file in request`, без перезагрузки |

После каждого отказа портал отвечал за 0,05–0,16 с, то есть перезагрузки не
было. `pio test -e native` — 160/160.

### Полный сценарий проверен на живом релизе

Реле стояло на 4.3.0, зеркало отдавало 4.3.1. Прогон целиком, каждый шаг
настоящий:

1. Скрипт, отданный самим устройством, прочитал настоящий `manifest.json` с
   зеркала и с настоящими фактами реле сказал `New version 4.3.1 available`,
   показав кнопку.
2. Собранный им запрос — `POST /ota_update?size=477216&md5=933d82fed069...`.
3. Образ скачан с зеркала: 477 216 байт, md5 сошёлся с манифестом.
4. Отправлен на устройство тем же запросом: `Update success` за 15,9 с.
5. Реле перезагрузилось и отвечает `FW={v:'4.3.1'}`, Wi-Fi и MQTT зелёные.
6. Повторная проверка на обновившемся устройстве: `Up to date`, кнопка скрыта.

Размеры образов из CI в 4.3.1 совпали с локальными байт в байт — прикрепление
платформы работает.

### Что осталось

- Открыть обе страницы в браузере: вёрстка подсказки на главной и хода загрузки
  проверялась только по HTML, а не глазами. Сама механика проверена целиком.

## Дефект 4.4.0: ESP-07S ложно отказывает в установке через портал

Обнаружено сразу после выпуска, проверкой манифеста против фактов живых
устройств. Затронут один образ из пяти.

| Устройство | Образ 4.4.0 | Свободно в фактах | Скрипт | Устройство приняло бы |
|---|---|---|---|---|
| 10.0.1.86 ESP-01 реле | 477 376 | 479 232 | ставит | да |
| **10.0.1.25 ESP-07S** | **479 280** | **479 232** | **ОТКАЗ** | **да** |
| 10.0.1.20 D1 mini реле | 477 376 | 2 662 400 | ставит | да |
| 10.0.1.23 D1 mini датчик | 475 264 | 2 666 496 | ставит | да |
| 10.0.1.22 ESP-01 датчик | 475 264 | 483 328 | ставит | да |

Причина — та самая четвёртая правка из плана по `.bin.gz`, которую в 4.4.0 не
делали. `facts()` до сих пор отдаёт `(getFreeSketchSpace() − 0x1000) & ~0xFFF`
— формулу обработчика GyverPortal, от которого мы отказались в 4.3.1. Наш
обработчик объявляет настоящий размер, и `Update.begin` сравнивает с полным
`getFreeSketchSpace()`:

```
updateStartAddress = end − round(size)  ≥  currentSketchSize
⟺  round(size) ≤ end − currentSketchSize = getFreeSketchSpace()
```

Для ESP-07S: `round(479280) = 483328 ≤ 483328` — образ помещается ровно. А
страница сравнивает нескруглённый размер с заниженным на 4 КБ числом и отказывает,
промахнувшись на 48 байт. Ровно та же арифметика, что убила установку 4.3.0,
только теперь ошибка не в устройстве, а в его же подсказке браузеру.

Исправление на две строки и скорее уменьшает образ, чем увеличивает:

- `facts()` отдаёт `ESP.getFreeSketchSpace()` без вычитания сектора;
- скрипт сравнивает округлённый вверх до 4 КБ размер: `((f.size+4095)&-4096) > FW.f`.

Обходной путь до исправления: ESP-07S обновляется через espota, там ограничение
не действует.

### Исправлено в 4.4.1

`facts()` отдаёт `ESP.getFreeSketchSpace()` без вычитания сектора, скрипт
сравнивает округлённый вверх размер. Размеры образов практически не изменились:
`esp07s_relay8` остался 479 280 (запас 2 000), у датчиков прибавилось 16 байт.

Проверено прогоном скрипта в node на трёх граничных случаях:

| Образ | Свободно | Результат |
|---|---|---|
| 479 280 (ESP-07S из релиза) | 483 328 | ставит |
| 483 328 (ровно по сектору) | 483 328 | ставит |
| 483 329 (на байт больше) | 483 328 | отказ, `Image needs 487424 B` |

Полный набор из 24 проверок скрипта проходит, `pio test -e native` — 160/160.
В самом наборе нашлось устаревшее ожидание: адрес POST сверялся без параметров,
хотя `?size=` и `?md5=` появились ещё в 4.3.1. Ожидание исправлено.

## Найдено при выпуске 4.4.1: предел OTA завышен на сектор

Проверяя, что ESP-07S теперь принимает обновление, наткнулись на большее.

**ESP-07S на 4.4.1 не принимает образ своего размера ни одним путём.** Проверено
на железе: espota вернула `ERROR[4]: Not Enough Space`, ничего не записав.
Плата при этом здорова и работает на 4.4.1.

Причина — арифметика, а не поломка. Истинное условие OTA:

```
round(новый) + round(текущий) ≤ 962560
```

При равных размерах это `round(X) ≤ 481280`, но 481280 не кратно 4096, поэтому
на деле `round(X) ≤ 479232`, то есть **X ≤ 479232**. А `custom_ota_max_size`
для однометровых плат стоит 481280 — на сектор больше. Полоса 479 233…481 280
проходит проверку сборки и не проходит на устройстве:

| Образ | round | пара | итог |
|---|---|---|---|
| 479 232 | 479 232 | 958 464 | влезает |
| **479 280** (наш ESP-07S) | 483 328 | 966 656 | **не влезает** |
| 481 280 (нынешний предел) | 483 328 | 966 656 | не влезает |

Образ ESP-07S — 479 280, то есть на 48 байт внутри запретной полосы. Опять 48.

### Что это значит

- `custom_ota_max_size` для `[esp01]` и `[esp07s]` должен быть **479 232**.
  Сейчас проверка сборки, заведённая как критерий именно возможности OTA
  ([ADR 0007](docs/adr/0007-enforce-ota-image-budget.md)), пропускает образы,
  которые обновиться не могут.
- Остальные образы в порядке: ESP-01 реле 477 376, датчик 475 280 — оба ниже
  479 232 и обновляются.
- ESP-07S до следующего релиза остаётся на 4.4.1. Чтобы он снова стал
  обновляемым по воздуху, следующий его образ должен быть меньше 479 232 —
  хотя бы на 48 байт.

### Отсюда меняется приоритет `.bin.gz`

Сжатие перестало быть ускорением и стало способом удержать самую большую плату
обновляемой. Со сжатым образом стейджится ~330 КБ вместо 483 КБ: `round(gz)`
укладывается в свободные 479 232 с запасом полутора сотен килобайт, и полоса
перестаёт существовать. Механика проверена на реле ещё 2026-08-07, а свой
обработчик из 4.3.1 объявляет настоящий размер, поэтому путь через портал для
`.gz` теперь безопасен.

## Публикация `.bin.gz`: сделано

Разбор и числа этого шага — в разделах выше; здесь итог. Решение записано как
[ADR 0014](docs/adr/0014-compressed-ota-images.md), выпуск — 4.6.0.

### Что изменилось

1. **Сборка** (`extra_script_ota.py`) кладёт `.bin.gz` рядом с `.bin`, а не
   релизный workflow: локальный `pio run` должен давать те же файлы, что и
   релиз. Заодно она проверяет вторую границу безопасности — распаковка не
   догоняет источник. Конец области скетчей берётся не константой, а из того
   самого ldscript, которым собран образ (`$LDSCRIPT_PATH`); у d1_mini его
   задаёт меню размера флеша, а не `board_build.ldscript`, поэтому спрашивать
   надо окружение, а не `BoardConfig`.
2. **Workflow** публикует `.bin` и `.bin.gz` в релиз и на зеркало. В манифесте
   основная запись образа стала сжатой: `file`/`size`/`md5` описывают `.bin.gz`,
   несжатый идёт полями `raw`/`raw_file`/`raw_md5`.
3. **Обработчик** (`lib/core/core_ota.h`) принимает `.bin.gz`, но только с
   настоящим `?size=`. Без размера работает прежнее правило `CustomOTA`, и
   распаковка затрёт свой источник, поэтому `.gz` без размера отвергается до
   `Update.begin()`. Ручная заливка файлом размер передать не может — руками
   заливается только `.bin`.
4. **Скрипт** (`lib/core/core_update.h`) ставит `.gz` и проверяет обе границы.
   Второй нужен конец области скетчей: он приехал в факты полем `FW.e` из
   линкерного символа `_FS_start`, а не из `getSketchSize()` — там чтение
   заголовка образа и обход сегментов, а здесь константа компоновки.

Правка про лишний сектор в `facts()` (пункт 4 старого плана) была не нужна:
её сделали ещё в 4.4.1.

### Форма манифеста выбрана по флешу

Названия вида `gz_size`/`gz_file`/`gz_md5` стоили бы лишних байт PROGMEM в
скрипте — на `esp07s_relay8` их нет. Со сжатой записью в основных полях скрипт
читает те же три поля, что и раньше, и добавляет одно (`raw`).

Побочно вышла совместимость со старым манифестом: там `file`/`size`/`md5` тоже
самосогласованы и описывают `.bin`, поля `raw` нет, вторая граница
пропускается — и такой образ ставится обычным несжатым путём.

### Замеры на 4.6.0

| Образ | raw | предел | запас | gz | доля |
|---|---|---|---|---|---|
| `esp01_relay` | 477 360 | 479 232 | 1 872 | 329 502 | 69,0 % |
| `esp07s_relay8` | 479 232 | 479 232 | **0** | 331 114 | 69,1 % |
| `esp01_ds18b20` | 474 992 | 479 232 | 4 240 | 328 434 | 69,1 % |
| `d1_mini_relay` | 477 360 | 1 044 464 | 567 104 | 329 511 | 69,0 % |
| `d1_mini_ds18b20` | 474 992 | 1 044 464 | 569 472 | 328 445 | 69,1 % |

Весь gz-путь, кроме второй границы, вышел бесплатным по флешу: перестройка
манифеста окупила и приём `.gz`, и установку сжатого. Вторая граница в рантайме
(`FW.e` плюс её проверка в скрипте) стоит ровно 64 байта и съедает весь остаток
`esp07s_relay8`. Решено оставить: 64 байта запаса всё равно не переживают ни
одной содержательной правки — прикрепление платформы в своё время стоило 3,3 КБ,
— а это последняя проверка перед тем, как за дело возьмётся eboot, и считает
она по разметке образа, который на устройстве действительно работает.

### Выкат шёл в две фазы, вторая включена в 4.7.1

Найдено уже после реализации: совместимость манифеста односторонняя. Новый
скрипт старую форму понимает (поля `raw` нет — ставит несжатым), а прошивки до
4.6.0 новую форму не понимают никогда: они берут `file`, скачивают `.bin.gz` и
отвергают его своим же `not a .bin`. Скрипт лежит во флеше устройства, заменить
его может только само обновление.

Поэтому релиз, который приносит в парк умеющий gz скрипт, публикует манифест
ещё в старой форме. Форму переключает `COMPRESSED_MANIFEST` в релизном
workflow: `false` в 4.6.0, `true` в первом релизе после того, как 4.6.0
разошлась по парку (сделано в 4.7.1). `.bin.gz` публикуется с самого начала,
в фазе 1 он просто
никем не запрашивается.

Проверено на харнессе всеми четырьмя сочетаниями скрипта и формы манифеста.
Цена ошибки была бы не кирпич, а ручной обход парка: устройство осталось бы на
прежней прошивке с ошибкой обновления.

Отдельно про ESP-07S: он сидит на 4.4.1 размером 479 280, свободно у него ровно
479 232, а образ 4.6.0 — тоже ровно 479 232. Влезает впритык и только несжатым,
каким путём его ни доставляй: сжатый образ эта прошивка принять не умеет. Запас
у него появится только с 4.7.0.

### Чего ждать и чего не ждать

Передача 477 → 330 КБ, обновление через портал заметно быстрее. Потолок OTA
формально сдвигается к ~565 КБ, но воспользоваться этим сразу нельзя: пока в
релизе лежит и несжатый `.bin`, поднимать `custom_ota_max_size` нельзя —
ручная заливка файлом упрётся в прежнюю границу. Это открытая дверь, а не
готовая прибавка.

### Что осталось сделать руками

- Пройти матрицу приёма образа из runbook на живой однометровой плате. Восемь
  строк, две из них новые (`.gz` с размером и `.gz` без размера). Проверка
  сжатой заливки обязательна именно на 1 МБ плате: ошибка в объявленном размере
  там приводит не к отказу, а к затиранию источника и лечится только UART.
- ~~Переключить `COMPRESSED_MANIFEST` на `true`~~ — сделано в 4.7.1. Порог
  совместимости оказался 4.6.0, а не последняя версия: сжатый манифест понимает
  любая прошивка, где gz умеют и скрипт, и приёмник образа. Матрица «скрипт
  устройства × форма манифеста» прогонялась на опубликованных артефактах —
  отказывает только 4.5.1 и старше.
- Двигать `custom_ota_max_size` однометровых плат пока не следует, и 4.7.0
  этого не потребовала: см. ниже, цена оказалась выше ожидаемой.

## 4.7.0: выбор версии и «что нового» на странице обновления

Решение и отвергнутые пути — в [ADR
0015](docs/adr/0015-release-history-on-update-page.md).

### Почему скрипт уехал на зеркало

Обе фичи — это несколько строк JavaScript, и упёрлись они не в логику, а в
флеш. Замеры на `esp07s_relay8`:

| Вариант | Образ | Запас |
|---|---|---|
| 4.6.0, как выпущено | 479 232 | 0 |
| обе фичи, скрипт в PROGMEM | 479 648 | −416 |
| обе фичи, скрипт на зеркале | 477 744 | +1488 |

Запаса не было ни байта, то есть не помещалась не только пара фич, а вообще
что угодно. Оставались две развилки: поднять `custom_ota_max_size` или убрать
скрипт из флеша.

Подъём предела просчитан и отвергнут. Предел равен `floor((962560 / 2) / 4096)
* 4096` и держит свойство «любой образ заменяется любым». Ближайшее значение
выше — 483 328, но `Update.begin()` сравнивает округлённый размер со свободным
местом, и уже на 481 280 округление даёт 483 328 при 479 232 свободных.
То есть безопасного маленького шага не существует: любой подъём выше 479 232
убивает espota и ручную заливку `.bin` на однометровых платах разом. Остаются
кнопка `Install` со сжатым образом и UART, и вернуться можно только UART на
каждом устройстве.

Переезд скрипта на зеркало освободил 1881 байт вместо трат. Предел не тронут,
espota и `tools/fleet_upload.py` работают как работали.

### Что сделано

- `web/fw.js` — скрипт страницы, публикуется на зеркало релизным workflow.
  `lib/core/core_update.h` отдаёт только тег `<script src=…>` с `onerror`,
  который пишет `Offline` в строку состояния. Маршрута `/fw.js` больше нет.
- `CHANGELOG.md` и `tools/changelog.py` — секция на версию. Уходит и в тело
  релиза, и в поле `notes` манифеста. Релиз падает до сборки, если секции нет.
- `manifest.json` получил `history`: пять релизов, свежий первым, у каждого
  свои `notes` и свой набор образов. Верхние `version`/`images` дублируют
  первую запись — на них живут прошивки 4.3.1–4.6.0.
- Релизный workflow приносит ассеты четырёх прошлых релизов и сам делает им
  `.bin.gz`. У релизов до 4.6.0 сжатой формы в ассетах нет вовсе, а без неё
  откат на них с крупного образа невозможен.
- `test/web/fw_test.js` — стенд скрипта, отдельная работа в CI.

### Замеры на 4.7.0

| Образ | Образ, байт | Предел | Запас | `.gz` | Доля |
|---|---|---|---|---|---|
| `esp01_relay` | 475 840 | 479 232 | 3 392 | 328 788 | 69,1 % |
| `esp07s_relay8` | 477 744 | 479 232 | 1 488 | 330 451 | 69,2 % |
| `esp01_ds18b20` | 473 488 | 479 232 | 5 744 | 327 736 | 69,2 % |
| `d1_mini_relay` | 475 840 | 1 044 464 | 568 624 | 328 783 | 69,1 % |
| `d1_mini_ds18b20` | 473 504 | 1 044 464 | 570 960 | 327 760 | 69,2 % |

## 4.7.1: вторая фаза сжатого манифеста

`COMPRESSED_MANIFEST` переключён на `true` после того, как 4.7.0 разошлась по
парку. Прошивка не менялась — только форма записи в манифесте зеркала.

### Порог совместимости — 4.6.0, не последняя версия

Это стоило проверки, потому что определяет, кого флип отрезает. Проверено по
коду тегов и прогоном матрицы «скрипт устройства × форма манифеста» на
опубликованных артефактах:

| Прошивка | Скрипт | `core_ota.h` | Сжатый манифест |
|---|---|---|---|
| 4.5.1 и старше | во флеше | только `.bin` | **отказ `not a .bin`** |
| 4.6.0 | во флеше | `.gz` с `?size=` | ставится |
| 4.7.0 и новее | с зеркала | `.gz` с `?size=` | ставится |

4.6.0 понимает сжатый манифест без доработки — ради этого она и выпускалась
(ADR 0014). Сделать флип совместимым и с 4.5.1 нельзя: обе версии читают из
манифеста одни и те же `file`/`size`/`md5`, и различить их нечем. Такие
устройства остаются на ручной заливке `.bin` через портал.

### Почему риск распаковки невелик

`eboot` распаковывает gzip начиная с ядра 2.6.0, и на устройстве он тот,
который приехал последним по воздуху: образ ESP8266 несёт `eboot` первыми
~4 КБ (у `esp01_relay` и `esp01_ds18b20` первое расхождение только на байте
4109), а `copy_raw` пишет staged-образ с адреса 0, то есть перезаписывает и сам
загрузчик. Парк на 4.7.0 собран прикреплённой платформой 4.2.1 (ядро 3.1.2),
где `uzlib` в `eboot.c` на месте.

Остаточный риск — само железо: сжатая заливка ни разу не проверялась на
устройстве. Сквозная проверка md5 ловит порчу при передаче до записи, но не
ошибку распаковки при загрузке.

### Что осталось проверить на железе

- Первую сжатую установку. Дешевле всего одной строкой, до всякого релиза:
  файл берётся с зеркала, размер и md5 — те же, что уйдут в `?size=`/`?md5=`:

  ```bash
  M=https://mr-whitefoot.github.io/ESP-Device-Portal
  curl -sO $M/ESP_Relay_v4.7.0.bin.gz
  curl -F "firmware=@ESP_Relay_v4.7.0.bin.gz" \
    "http://<ip>/ota_update?size=328788&md5=86369b188ec056f43ad9b994ff128ec7"
  ```

  Это строка 4 матрицы приёма образа из runbook. Ожидается `Update success`,
  затем перезагрузка и версия 4.7.0 в `Information`; `flash=` в баннере обязан
  остаться прежним — для `.gz` `Updater` байты режима не патчит.
- Выбор версии и «что нового» в браузере: список из пяти релизов, своя версия
  помечена `(installed)`, переключение меняет текст.
- Откат на прошлую версию с однометровой платы. В несжатой форме манифеста
  (фаза 1) откат с 4.7.0 на 4.4.1 идёт впритык: 483 328 при 483 328 свободных.
- `Offline` в блоке обновления при подключении к точке доступа устройства.

## Инцидент: датчик ESP-01 10.0.1.22 не поднялся после 4.3.1

**Статус: не разобран, устройство недоступно.** Записано по горячим следам.

### Что известно

- Массовая загрузка 4.3.1 прошла по флоту. Реле D1 mini и ESP-07S после
  заливки ответили `HTTP 200` и показали версию `4.3.1` — их проверка в логе
  утилиты есть.
- Датчик `esp01_ds18b20` (10.0.1.22): заливка дошла до 100 % и espota вернула
  **`Result: OK`** в 12:37:14, `SUCCESS` за 17,78 с. Дальше
  `[sensor_esp01] verification: <urlopen error timed out>` — портал не поднялся
  за 30 секунд. Итог прогона: `4 ok, 1 failed, 1 not attempted` (прогон
  прервался, `sensor_d1_mini` в нём не участвовал и был обновлён отдельно).
- Сейчас устройство молчит полностью: ping — 100 % потерь, HTTP не отвечает,
  порт 8266 закрыт, в ARP-таблице запись `(incomplete)`, то есть в сети его
  нет вовсе.
- Поднялась ли точка доступа `ESP Temperature AP`, проверить не удалось:
  сканер сетей на macOS список не отдаёт. **Это первое, что нужно выяснить** —
  поднятая AP означает, что прошивка жива и вопрос только в Wi-Fi.

### Что этим инцидентом не объясняется

Свой обработчик приёма образа из 4.3.1 (`lib/core/core_ota.h`) к делу
непричастен: espota идёт через `ArduinoOTA` и `Updater`, а их 4.3.1 не
трогала. Путь, по которому заливали, в этой версии не менялся.

Что в 4.3.1 действительно новое для espota-пути:

- **платформа 3.2.0 → 4.2.1** (Arduino core 3.0.2 → 3.1.2). Вместе с ней
  меняется eboot, зашитый в первый сектор образа, и SDK. Правда, тот же
  переход реле ESP-01 10.0.1.86 пережило многократно;
- **GyverDB разрешился в 1.4.4** вместо прежней 1.4.3: в `lib_deps` стоит
  `^1.4.3`, версия плавающая. У датчика самая старая база настроек во флоте.

### Проверка `tools/fleet_upload.py` — утилита чиста

Прочитана целиком. Инвентарь связывает `sensor_esp01` → 10.0.1.22 →
`esp01_ds18b20`; в `execute()` для каждого устройства запускается
`pio run -e <его окружение> -t upload --upload-port <его host>`. Подмены
образа нет, устройства идут строго последовательно, реле раньше датчиков
(стабильная сортировка по `kind`). Перед заливкой делается HTTP-preflight — в
логе он прошёл, датчик был жив.

Заголовки собранных образов тоже ни при чём: `ESP_DS18B20_v4.3.1.bin` и
`ESP_Relay_v4.3.1.bin` начинаются одинаково, `e9 02 00 20` (1 МБ), а реле
ESP-01 с таким заголовком прошивалось многократно.

### Решающее наблюдение: живы все, кроме одного сочетания

Опрос флота после инцидента:

| Адрес | Устройство | Состояние |
|---|---|---|
| 10.0.1.21 | ESP-01 реле | жив, HTTP 401 (портал под авторизацией) |
| 10.0.1.86 | ESP-01 реле | жив, 4.3.1 |
| 10.0.1.20 | D1 mini реле | жив, 4.3.1 |
| 10.0.1.25 | ESP-07S банк | жив, 4.3.1 |
| 10.0.1.23 | **D1 mini DS18B20** | жив, 4.3.1 |
| 10.0.1.22 | **ESP-01 DS18B20** | молчит |

То есть на 4.3.1 нормально работают и другой ESP-01, и другой DS18B20. Умерло
единственное устройство, где сходятся оба: **ESP-01 с шиной OneWire на GPIO2**.
А GPIO2 у ESP-01 — загрузочный пин, он обязан быть высоким в момент сброса.
Это единственное железное отличие погибшего устройства от пяти выживших.

### Окно отказа: только два шага

`Result: OK` означает больше, чем «файл ушёл». espota передаёт md5 в
приглашении (`ArduinoOTA.cpp:184`), `Update.end()` сверяет его по принятым
байтам и только при совпадении записывает команду eboot. То есть образ дошёл
целиком, целостность подтверждена, задание загрузчику записано. Передача и
повреждение файла исключены полностью.

Остаётся ровно два шага, и оба уже без сети:

1. копирование в eboot из области стейджинга в нулевой адрес (несколько секунд,
   переписывается в том числе сектор самого загрузчика);
2. первая загрузка нового образа.

Код устройства при этом чист: `device::begin()` выставляет
`pinMode(ONE_WIRE_PIN, INPUT)` и линию вниз не тянет ни при старте, ни между
опросами.

### Гипотезы, от вероятной к менее

1. **Загрузочный режим по GPIO2.** После OTA прошивка делает `ESP.restart()` —
   тёплый сброс, при котором линия могла остаться притянутой вниз шиной
   OneWire или самим датчиком. Модуль уходит не в тот режим загрузки и
   выглядит мёртвым, оставаясь исправным. Согласуется с тем, что погибло
   ровно то устройство, где OneWire сидит на загрузочном пине. **Лечится
   снятием и подачей питания — проверить в первую очередь, это бесплатно.**
2. **Прерванное копирование в eboot.** Между записью команды и концом
   `copy_raw` проходит несколько секунд, в течение которых переписывается в
   том числе нулевой сектор с загрузчиком. Просадка питания в этом окне
   оставляет флеш недописанным. Лечится только UART.
3. **Прошивка уходит в цикл перезагрузок** до подъёма Wi-Fi — тогда нет ни
   сети, ни точки доступа. Против этой версии то, что тот же код с теми же
   версиями библиотек (`OneWire 2.3.8`, `DS18B20 0.2.7`) нормально работает на
   D1 mini DS18B20. Видно только по serial.
4. Совпадение с аппаратной неисправностью питания или самого модуля.

### Разбор по UART: OTA ни при чём, модуль — ESP8285 в режиме QIO

Плата снята с датчика и подключена через USB-UART (`/dev/cu.usbserial-110`,
CH340). Собранные факты:

- **Чип отвечает загрузчику.** `esptool flash_id`: `Chip is ESP8285`,
  `Features: WiFi, Embedded Flash`, `Crystal is 26MHz`, `MAC
  b4:60:ed:48:fc:73`, `Detected flash size: 1MB`. То есть это **не обычный
  ESP-01 с внешней флеш-памятью, а ESP8285 со встроенной**.
- **Загрузка идёт из флеша, перемычки в порядке.** По сбросу ROM печатает
  `rst cause:2, boot mode:(3,1)` — режим 3 это загрузка из флеша. Версия про
  GPIO2 и загрузочный пин исключена: датчик снят, а режим правильный.
- **Образ во флеше цел.** Прочитаны все 475 136 байт области приложения и
  сверены с `ESP_DS18B20_v4.3.1.bin`: **совпадение побайтное**, хвост `0xFF`.
  eboot скопировал безупречно, заголовок на чипе тот же, что в файле.
- **Системная область не пуста**: секторы 252–255 содержат данные калибровки.
- **Отказ детерминирован.** После каждого сброса ровно 244 байта: SDK печатает
  `rf cal sector: 252`, `freq trace enable 0`, `rf[112] : 0` — и останавливается.
  Баннер прошивки `boot start` не появляется ни разу: до `setup()` дело не
  доходит, всё умирает на инициализации радио.

Итого: передача проверена md5 самим устройством, копирование проверено чтением
обратно, перемычки верны, флеш цел. **Обновление ничего не испортило.**

### Причина: образ собран в QIO для чипа, которому нужен DOUT

Профили PlatformIO:

| board | flash_mode |
|---|---|
| `esp01_1m` (используем) | **qio** |
| `esp8285` | **dout** |

Встроенная флеш-память ESP8285 работает только в DOUT: линии, которые QIO
занимает под два дополнительных бита, у неё разведены иначе. Заголовок на чипе —
`e9 02 00 20`, где байт 2 = `0x00` = QIO; для этого модуля должно быть `0x03`.

Почему сломалось только сейчас: `Updater` при OTA переписывает байт режима из
конфигурации **работающей** прошивки, то есть ошибка воспроизводит саму себя и
по воздуху не исправляется. Прежний SDK её терпел, а с переходом 4.3.1 на
платформу 4.2.1 (Arduino core 3.0.2 → 3.1.2) SDK стал падать на первом же
обращении к флешу за данными калибровки — ровно там, где мы и встали.

Это объясняет и то, почему остальные пять устройств живы: у них, судя по всему,
настоящие ESP-01 с внешней флеш-памятью, которым QIO подходит.

### Плата починена, диагноз подтверждён

По UART залит **тот же самый образ** с `--flash_mode dout`; изменился ровно один
байт заголовка, `0x00` → `0x03`. Этого хватило:

```
[    0.082] I boot start Temperature v4.3.1 built=07.08.2026
[    0.083] I boot chip=48fc73 heap=44576 reset=External System
[    0.094] I dev  ds18b20 init bus=2 period=10s
[    0.094] W dev  ds18b20 not found on the bus     <- датчик снят, так и должно
[    4.437] I wifi connected ip=10.0.1.22 rssi=-60
[    4.448] I boot ready heap=40720
[    5.086] I mqtt connected
```

Устройство вернулось в сеть на прежний адрес, портал отдаёт
`FW={v:'4.3.1',i:'DS18B20',f:483328}`, оба индикатора зелёные. Настройки целы:
писалась только область приложения, LittleFS на `0xEB000` не тронут — SSID,
брокер и имя устройства сохранились. Заголовок на чипе теперь `e9 02 03 20`.

Тем самым причина доказана экспериментом: **дело было исключительно в режиме
флеша**, и обновление 4.3.1 к поломке отношения не имеет.

Осталось вернуть датчик на место и убедиться, что температура читается.

### Сделано в репозитории после починки

Обе правки внесены, обе уложились в запас.

**Профиль `[esp01]` переведён на `dout`** (`board_build.flash_mode`). Размер
образов не меняется — это байт заголовка. Проверено по собранным файлам:
заголовки ESP-01 стали `e9 02 03 20` вместо `e9 02 00 20`, D1 mini и ESP-07S
остались как были. Профиль `[esp07s]` намеренно не тронут: тип чипа на той
плате не проверялся.

**Режим флеша печатается в загрузочном баннере** полем `flash=`. Цена:

| Образ | было | стало | осталось |
|---|---|---|---|
| `esp01_relay` | 477 216 | 477 376 | 3 904 |
| `esp07s_relay8` | 479 104 | 479 280 | **2 000** |
| `esp01_ds18b20` | 475 104 | 475 264 | 6 016 |

Плюс 160–176 байт; у самого узкого образа осталось 2000 байт.
`pio test -e native` — 160/160, все шесть окружений собираются.

**Чего проверить не удалось:** увидеть новый баннер на живом устройстве. Реле
не подключено к serial, а по HTTP кольцо лога не поймать — `portalCheckForm()`
вызывает `portal.updateLog()` на каждом опросе портала, и открытая в браузере
страница осушает кольцо раз в секунду. Частый опрос из curl гонку не выиграл.
Косвенно: строки ` flash=` и `QIO/QOUT/DIO/DOUT` присутствуют в собранном
образе. Увидеть баннер глазами получится при следующем подключении любой платы
к UART или если закрыть портал в браузере и перезагрузить устройство.

### Шина датчика D1 mini переехала на D2/GPIO4

В рабочей копии несколько дней висела незакоммиченная правка
`ONE_WIRE_PIN` с `D4` на `D2`. Опрос живого устройства показал, что она не
эксперимент, а недостающее исправление: датчик D1 mini (10.0.1.23) отдавал
`--` вместо температуры, то есть шина физически уже была на D2, а прошивка
искала её на D4. Для сравнения ESP-01 (10.0.1.22) после возврата датчика на
место показывал 19,9 °C.

Правка принята, и под неё приведены все документы: `platformio.ini`,
`ARCHITECTURE.md`, `README.md` (таблица окружений, схема подключения, команда
сборки, раздел про выбор устройства), `docs/runbooks/verification.md` и
[ADR 0010](docs/adr/0010-board-profiles.md). Проверено, что компилятор получает
именно `-D ONE_WIRE_PIN=D2`.

Побочная польза: GPIO2 — загрузочный пин ESP8266, и внешней шине на нём делать
нечего. У ESP-01 выбора нет, а на D1 mini он есть.

### Что делать с репозиторием
1. **Перевести профиль `[esp01]` на `dout`.** Режим работает и на настоящих
   ESP-01, и на ESP8285, ценой небольшой потери скорости чтения флеша. Важная
   оговорка: **по воздуху это ничего не исправит**. `Updater` переписывает байт
   режима из конфигурации работающей прошивки, поэтому OTA всегда сохраняет
   текущий режим устройства. Правка защищает только тех, кого прошивают по
   UART, — то есть новые платы и восстановленные.
2. **Печатать режим флеша в загрузочном баннере.** Сейчас узнать, какой режим у
   устройства, можно только сняв его и подключив UART. `ESP.getFlashChipMode()`
   стоит порядка полусотни байт, а после этого `curl http://<ip>/log` отвечает
   на вопрос «кто ещё в QIO на однометровом чипе» для всего флота сразу. Это
   единственный дешёвый способ найти остальные ESP8285, не разбирая их.
3. Пока такой диагностики нет, оставшиеся устройства — ESP-01 реле 10.0.1.21 и
   10.0.1.86 — под тем же риском: они живы на текущем SDK, но следующая смена
   платформы может повторить историю. Проверять по одному через `esptool
   flash_id` при ближайшей возможности снять их.

### Отменённая версия: загрузочный пин GPIO2

Самый дешёвый опыт, который делит гипотезы пополам: **отключить DS18B20 от
модуля и подать питание заново**. У ESP-01 GPIO2 обязан быть высоким в момент
сброса; ниже — не загрузочный режим, а тихий ступор без единого признака жизни,
ровно то, что мы видим. На этом же пине висит шина OneWire, и притянуть её вниз
может отказавший датчик или повреждённая проводка.

Отсюда важное следствие: **OTA могла не убивать устройство**. Прошивка работала
долго без перезагрузок, а обновление всего лишь вызвало первый за долгое время
сброс — и он вскрыл неисправность, которая до него никак себя не проявляла.

- модуль поднялся без датчика (появилась `ESP Temperature AP`) — флеш цел,
  виноваты датчик или проводка;
- не поднялся и без датчика — испорчено содержимое флеша, лечится только UART.

### Что собрать, прежде чем чинить

- Хвост вывода espota для датчика: `Result: OK` или конкретная ошибка.
- Появилась ли сеть `ESP Temperature AP` (посмотреть с телефона).
- Что говорит устройство в serial на 74880: баннер загрузки, `reset=`,
  сообщения eboot. У ESP-01 это требует USB-UART переходника.

### Восстановление

USB-UART, GPIO0 на землю в момент подачи питания, заливка `esptool` — порядок
описан в README, раздел «Первая прошивка чистого модуля». Пока причина
неизвестна, **на оставшиеся датчики 4.3.1 не отправлять**.
